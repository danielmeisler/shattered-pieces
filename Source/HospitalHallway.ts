namespace Endabgabe {
    export async function HospitalHallway(): ƒS.SceneReturn {
        console.log("FudgeStory HospitalHallway Scene starting");
        let menu = ƒS.Menu.create(menuItems, menuButtons, "menu");
        menu.open();

        // Speeches
        let text = {
            Protagonist: {

            },
            Sumi: {
                S3200_01: "",
            },
            Nobu: {

            }
        };

        // Decisions

        // Start
    }
}